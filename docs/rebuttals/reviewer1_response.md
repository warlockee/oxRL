# Response to Reviewer

**Global response**

We thank the reviewer for the careful reading and for recognizing the extensiveness of the experiments, the importance of the problem, and the reproducibility of the release. During the discussion period we ran **new experiments answering each main request**: (1) a **Qwen2.5-32B** scale point; (2) the **fixed-dataset control replicated on Gemma 3**; (3) the **missing 3B cells of Table 1**; (4) a **multi-seed rerun of the entire 0.5B grid** resolving the zero-variance question. All per-seed values are in the results appendix accompanying this response; raw evaluation JSONs ship with the released artifacts. **Every presentation fix below and all new results are already applied to the revised manuscript** (new Appendix: "Discussion-Period Additions"; revised Table 1).

**Headline — the requested 32B run completes the inversion.** With the identical deployment protocol (self-play data generated by the 32B model itself, shared hyperparameters, GSM8K 8-shot strict-match):

| Qwen2.5-32B (N=3) | Mean ± σ (%) | Δ vs Base |
|---|---|---|
| Base (Instruct) | 80.52 | — |
| SP-RFT | 80.82 ± 0.23 | +0.30 |
| DPO | 82.23 ± 0.04 | +1.71 |
| **SimPO** | **83.42 ± 1.14** | **+2.90** |

SimPO — the *worst* method at every scale ≤1.5B — is *best* at 32B; SP-RFT — the best small-scale method — gains a statistically zero +0.30 ± 0.23 over base, and DPO > SP-RFT is significant at p < 0.01 (Welch). The ordering holds in every seed, and per-method seed variances reproduce the paper's signatures at the new scale (DPO σ = 0.04, most stable; SimPO σ = 1.14, least). The RFT ceiling closes exactly as the paper's mechanism predicts: SP-RFT's gain over base falls +23.2pp (1.5B) → +1.6 (7B) → +0.3 (32B), and at 32B only 816 of 7,473 prompts still yield informative preference pairs (vs 1,332 at 14B). Small-scale selection is anti-predictive precisely for the methods practitioners would discard early. Per-seed values and raw eval JSONs: https://anonymous.4open.science/r/nips-dnb-artifact-2026-883E (`results/RESULTS_SUMMARY.md`; raw JSONs in `results/`; frozen configs in `release/configs/`).

**One clarification: GRPO is already a first-class method in the study.** §3 states "SGRPO (token-level GRPO with on-policy rollouts, exact-match reward, group-normalized advantages, λ_KL=0) is the online-RL representative in all main tables," with GSPO/CISPO and KL ablations in the appendix. SGRPO appears in Table 1 at every scale (best method at 1.5B: 58.0%). Table 1 now reads "SGRPO (GRPO)" and the introduction spells out Group Relative Policy Optimization at first mention (applied in the revision).

**On the rating rationale.** We gently note a tension: the rationale cites "inadequate reproducibility," while the review's Reproducibility field says "Yes — the authors released code and a training dataset as well as detailed hyperparameters" (520 frozen per-run configs, ~145k-row datasets, per-seed checkpoints, one-command train/eval, datasheet + Croissant metadata). Similarly, no technical flaw is identified, and the ethics assessment is "no or very minor concerns." We hope the final assessment can reconcile these with the score.

---

**W1 — Presentation (all applied in the revision).** (1) Full algorithm names + citations now appear at first mention in the introduction (Direct Preference Optimization, Simple Preference Optimization, Kahneman-Tversky Optimization, Odds-Ratio Preference Optimization, Group Relative Policy Optimization). (2) Abstract "≤1.5B" vs intro "≤3B": the reviewer is right this was confusing. SP-RFT leads DPO at 0.5B–3B — at 3B at *every* LR tested (55.7 vs 34.6 at per-algorithm best LR) — so the abstract now reads "≤3B" with the 3B caveat attached. (3) "Capable base model → useful one" is replaced with a precise statement: post-training converts raw problem-solving ability into reliably formatted, instruction-following behavior (base Qwen2.5-3B: 62.9% flexible-extract vs 10.7% strict-match). (4) "Formatting capabilities" is replaced by *answer-format compliance under strict-match grading*, cross-referenced to the strict-vs-flexible gap appendix. (5) Naming normalized to Qwen2.5-14B etc. throughout.

**W2 — Evidence.** (1) *ARC-C/multiple-choice results are in the submission*: per-benchmark tables at 1.5B and 7B, a cross-scale summary appendix, and the 14B general-average column; cited from abstract, intro, §4.6, Limitations. We will move the 1.5B table into the main text, and add Gemma-3 1B/4B/12B general results (spreads ≤1.6pp, no inversion). (2) *SGRPO < base at 3B* is the documented pathology; the appendix explains the mechanism (on-policy rollouts get zero reward for ill-formatted generations, so the crisis compounds: 4.93% ep1, 8.11% ep2 vs base 10.69%). We will surface this in the Table 1 caption. (3) *IPO > SP-RFT at 0.5B*: +0.53pp at the paper's documented noise floor. Our new multi-seed rerun quantifies it: IPO 33.97±0.68 vs SP-RFT 33.59±0.26 — within one seed-σ; the four stable methods cluster within 0.79pp and we will state that no 0.5B ordering is meaningful. (4) *§4.4 format-compliance claim*: supported by the 36× (MATH) and 41× (general) spread compressions and the strict-vs-flexible table; cross-references will be moved to the sentence itself.

---

**W3.1 — 32B**: see Global response table (all cells N=3 seeds). The deployment picture now spans 7B→32B in Qwen plus 12B in Gemma; the DPO–SP-RFT gap is +6.0pp (7B) → +2.2 (14B) → +1.4 (32B), the ordering stable at every deployment scale and every seed.

**W3.2 — GRPO**: covered in the Global response — SGRPO *is* GRPO (DeepSeekMath token-level clipped surrogate), in Table 1 at all scales, with §4.7 online-vs-offline analysis and appendix KL/GSPO/CISPO ablations. The reviewer's W2 itself discusses our SGRPO results, so we read this as a presentation issue and will fix it as such.

**W3.3 — Fixed-dataset control beyond Qwen-1.5B**: the reviewer's caution was warranted; we ran the control on Gemma and it does **not** transfer unchanged. Gemma-3-1B, 3 seeds, identical hyperparameters; own-data checkpoints re-evaluated under the identical protocol:

| Gemma-1B trained on | SP-RFT | DPO | Winner |
|---|---|---|---|
| own (1B) data | **26.91 ± 0.55** | 22.82 ± 2.08 | SP-RFT +4.1 |
| 12B-generated data | 21.41 ± 1.01 | **24.69 ± 1.01** | DPO +3.3 |

Unlike Qwen (where 14B data preserves SP-RFT > DPO: 58.4 vs 53.3), on Gemma strong-teacher data flips the ordering — consistent with a capability-gap effect (SP-RFT imitates trajectories the 1B cannot reproduce; DPO's contrastive signal degrades gracefully). We will scope §4.7 to Qwen, add this control with discussion, and note it reinforces the paper's central message: rankings are unstable across scale, architecture, *and data provenance* — validate under your own workflow. We thank the reviewer for pushing on this.

---

**Q1 — Missing Table-1 values.** The "---" cells were IPO/KTO/SimPO at 3B, omitted because the 3B default-LR pathology made strict-match uninformative and the LR sweep covered the claim-bearing methods. Now filled (3 seeds, shared default LR): IPO 24.92±0.85, KTO 23.68±0.87, SimPO 9.60±3.84 — all far below base's 62.9% flexible-extract, confirming a checkpoint-wide pathology. These cells and the reason for any remaining "—" (SGRPO at 14B: ~6× rollout compute) are in the revised appendix.

**Q2 — Qwen3.** We cannot claim it without running it, and we deliberately do not. The mechanisms we identify (initialization dominance: 15.7→1.07pp spread compression at 1.5B; format-compliance regime at small scale) are not tied to Qwen 2.5 internals and replicate on Gemma 3. Because Qwen3's instruct initialization differs substantially, Finding 2 predicts the initialization effect will again dominate the algorithm effect — a falsifiable prediction we will state in the Discussion.

**Q3 — 5 vs 3 seeds.** Seed budget followed the statistical load: N=5 where headline significance tests live (7B core methods, p<0.001 after Bonferroni; the 20-variant 1.5B study = 100 runs), N=3 for supporting grids (1.5B core, IPO/KTO-7B, 14B, Gemma, controls), N=1 only for footnoted exploratory cells. Per-cell N is printed in every table; a sentence in §3 will state the policy.

**Q4 — Zero variance at 0.5B.** Base rows have no ± because ± is variance over *training* seeds and base models undergo no training. The σ=0 cells were real, separately-trained runs that produced bit-identical accuracies: in the framework version used for the March runs the data sampler received a fixed default seed, so runs differing only in `run.seed` were bit-reproducible (fixed in oxRL v1.9.0; the 1.5B/7B tables already use post-fix reruns). We re-ran the **entire 0.5B grid** under the fixed mechanism (Table-1 protocol): SP-RFT 33.59±0.26, DPO 33.18±0.31, IPO 33.97±0.68, KTO 33.38±0.98, SimPO 23.55±5.19. Variance is genuine and nonzero; original point estimates were accurate to <1pp; the exact SP-RFT=DPO coincidence disappears; SimPO's σ=5.19 independently replicates the paper's SimPO-instability finding at a third scale. The revised Table 1 now carries these numbers, and the mechanism is documented in the Multi-Seed Verification appendix.

**Dataset assessment.** The release includes a full Gebru-et-al. datasheet (confidentiality, offensive content, human subjects, ethical review, licensing, maintenance — `DATASHEET.md`) and Croissant metadata (`croissant.json`). If the concern is the submission-form fields, we apologize and will mirror the datasheet answers there.

**Closing.** We have fixed every presentation issue, added the requested 32B scale point, replicated the fixed-dataset control on a second family, filled the missing cells, and resolved the zero-variance question with new multi-seed runs. Given the review's own assessments (reproducibility: Yes; ethics: no/very minor; no technical flaw identified), we respectfully ask the reviewer to consider raising the score.
